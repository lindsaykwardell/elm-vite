{
  "name": "create-elm-vite",
  "version": "0.0.7",
  "description": "CLI for initializing an Elm app with Vite",
  "main": "dist/index.mjs",
  "bin": "dist/index.mjs",
  "scripts": {
    "start": "npm run build && node dist/index.mjs",
    "build": "tsc && tsup src/index.ts --format esm"
  },
  "devDependencies": {
    "@types/node": "^17.0.7",
    "tsup": "^5.11.10",
    "typescript": "^4.4.4"
  },
  "dependencies": {
    "commander": "^8.3.0",
    "execa": "^6.0.0"
  },
  "engines": {
    "node": "^14.13.1 || >=16.0.0"
  }
}